if(A1_MKL)
    add_executable(bmark_sparse main_mkl.cpp)
    target_link_libraries(bmark_sparse PUBLIC ${LACLIB_NAME})
else()
    add_executable(bmark_sparse main.cpp)
    target_link_libraries(bmark_sparse PUBLIC ${LACLIB_NAME})
endif()
